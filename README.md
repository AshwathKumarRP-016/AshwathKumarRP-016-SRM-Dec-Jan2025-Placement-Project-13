# JSP Addition Program

A simple JSP-based web application that demonstrates the integration of HTML forms with Java logic in a single JSP file.

## 📋 Overview

This project implements a basic calculator that adds two numbers using JavaServer Pages (JSP) technology. It showcases the use of scriptlets to embed Java code directly within an HTML page, handling form submission and data processing in a single file.

## 📁 File Structure

```
project/
└── addition.jsp    
```


## 🧪 Usage

1. Enter two numbers in the provided input fields
2. Click "Calculate Sum"
3. View the result displayed on the same page
4. The JSP processes the request, calculates the sum, and returns the HTML response

